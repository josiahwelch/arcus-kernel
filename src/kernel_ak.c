// Josiah Welch
// 8/29/25
// Kernel c file

//NOTE: heavily influenced by https://github.com/chipsetx/Simple-Kernel-in-C-and-Assembly#

#include "define.h"
#include "keyboard.c"
#include <stdint.h>
#include "kern_utils_ak.h"

void main_ak(void){
	/*
	clear_ak();
	print_ak("Arcus kernel loaded!", 0);
	print_ak("Arcus OS is loading...", 1);
	enable_cursor(4,5);

	char char_read = ' ';
	int i = 0;
	char *msg = (char *)LINE_BUFF;
	msg = "        \0";
	while (1) {
		if (i > 7) {i = 0;}
		char_read = get_ascii_char();
		msg[i] = char_read;
		print_ak_buff(2);
		i++;
	}
	*/
}
